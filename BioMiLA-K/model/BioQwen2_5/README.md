未来的研究使用lora + 大模型的微调方式进行

先使用roco进行模型的预训练，让模型能够产生caption，其中llm进行lora微调

pre_caption_lora   先进行lora+大模型的预训练
chat_lora_model    使用lora的聊天模型
model   接下游模型


使用图文模型来进行roco caption的重构，一方面用好的模型显存不够，另一方阿里云直接调用api也不太行，
只能用文本的大模型，来帮助修改roco的caption


现在想到两种预训练方式，一种直接roco caption和qa caption一起进行预训练微调，最后接下游任务
另一种，先进行roco caption微调，之后再用qa caption微调

预训练阶段只需要进行训练即可，无需进行测试等操作

97：
    1. 用rocov2的caption微调bioQwen2.5模型   只能用float32，模型batch为8，初始loss2.8，看看loss能不能下来   收敛非常慢
    预训练可以将视觉encoder和mlp一起进行微调   llm进行冻结   要比单纯mlp的微调 loss下降的快，但是十轮loss下降了0.3也很慢
    二十多轮  loss从3降到了2.5   50轮2.443   不跑了 模型只会给标题，就算提问，也只是给标题
    2. 把现有的description和qa数据集作为第二阶段的预训练数据集，  qa有5.8w, description有4k
        第一种，llm冻结，使用lora + mlp + vision encoder进行微调  先测试第一种  训练参数9千万，总参数5.8亿
              使用第一阶段非常有必要，会使得第二阶段模型收敛非常快，感觉在之后的实验中，模型只需要10轮、甚至5轮
              的预训练即可，而第二阶段的模型预训练甚至也可以只使用10轮左右就可以   
              5轮loss降的还不错，降到了1.3 二十轮降到了0.994  不跑了 整体效果不错，只不过缺少标题数据，导致描述和标题
              生成一般，已进行解决   
        第二种，llm、vision encoder冻结，使用lora_llm + mlp进行微调  用之前相同的数据试试效果  训练速度能快一半
              训练了5epoch，效果不错，和对不使用冻结vision encoder的模型相比，没有什么明显区别，但训练快了一半
              试试聊天效果  还不错   OK，之后的实验使用lora_llm + mlp进行微调即可   视觉encoder不进行微调来减少耗费
              试试使用梯度累加的方式  batch为4,16个batch后进行一次更新，看看会不会效果要比之前好
              使用梯度累加，loss会很大，因为是对所有的更新梯度样本进行loss求和所以会大，训练速度略微慢与少batch的梯度更新
        之后可以试试累计梯度的方式，将更新batch扩展到64，在trainer中添加即可   
    3、加载梯度累加预训练47轮模型来进行下游微调，其中图像不经过数据增强 使用16个batch  slake   依然不太行，loss会崩掉
        使用的是lora微调，没有进行全部参数微调   再试试全部参数微调      
        需要将model和dataset都重新改，现在已经敲定之前的vqa搭建方式无法有效进行训练
        目前想到将答案进行文本的输出，输出后与正确答案进行匹配    model2输出的内容与真实标签进行匹配，loss下降的不错
        但是acc有问题   loss降到了0，但是acc也为0，说明这种相似度不太对，换成另一种方式，我的模型输出是长序列，
        而答案是短序列，长序列有一部分是与答案的短序列相同，则说明答案正确，而不是两个序列完全相同，试了，还是不行。
        换另一种方式先将output的answer转换为文本，再与真实文本label计算准确率 直接在训练时将language模型输出不太行
        使用推理模型no_grad试试能不能行   还是不太行，重新修改
        还是使用原始的方式，将model进行修改，看qwen的源码，貌似hidden_states的最后是在第一个，之前都是给的最后一个，
        看错了，不太行，根本收敛不了，试试冻结视觉模型的效果   稍微好一点但依然不太行，试试答案不平均，使用第0个位置的 
        还是不行
        如果使用lora模型则会在原始Base_model下多一个Model块儿进行封装，重新修改代码，直接从原始代码的model进行修改
        依然不太行，和之前的区别不大   
        直接将图像embedding和文本的hidden states点乘  冻结图像模型  最后接个一层的映射进行结果映射  还是会崩掉
        不冻结图像模型试试   依然不行   由于之前用InQwen就出现loss会崩掉的情况，因此推测是语言模型的问题，对语言模型
        的输出平均只平均答案部分   比之前好多了，效果非常不错，特别是open   操，跑的slake 才74% overall   不跑了
        可以再试试平均答案和视觉特征   slake   看测试集不太行   
        重新修改，继续使用平均答案部分，将平均答案加入aug数据增强再试试   slake 80多   rad 55多，
        进行修改，答案输出与视觉输出点乘输出试试    
            97  slake 收敛的比之前快，但效果区别不大，加入aug再试试 有提升，和之前区别不大   完成
            84  rad   效果有明显提升  61.5   加入aug    反而变差了   结束
            84  rad   使用答案输出不进行aug试试
            97  slake  使用仅答案输出的方式，使用普通数据增强，区别不大，不跑了
            97  rad   使用仅答案输出的方式，目前看没aug效果反而更好，rad需要答案和image_mean
        
97:
    预训练第二阶段148178的数据，其中至少包括一张图像所对应的数据，看看预训练后的效果会不会更好，特别是在交互对话上
    加入了caption数据，且由于使用的是之前构建的数据集，detailed description和reasoning description都使用了模型
    生成的问答的数据    保证每128个batch一轮，进行梯度累积反向传播
    已经预训练了35轮，直接传到84试试效果

预训练第二阶段仅需要使用3M数据


之前的数据集构建方式存在一定的问题，首先caption的问题不应该出现在第二阶段，其次，细节描述数据应该每个问题都是随机出现，而不是
一直固定，之后需要改代码，在数据加载的时候从一个问题集合进行选择。

35: 跑QA数据构建，数据集的构建其实存在很多问题，在数据集生成后，我们需要进行数据处理，转换数据使用qa_instruct_build.py
34: 跑description数据构建   有问题，目前，我们只生成了描述，却忽略了提问。应该让模型生成提问才对，操

接下游VQA的时候，可以试试不进行数据增强的效果

加载预训练两轮的lora模型试试聊天效果，11轮的效果还不错，但目前有一个问题，就是问描述的问题，detailed问题，他还是不知道问的什么，
然后重复回答内容。其次，两轮的模型不如11轮的，两轮生成的答案结果非常简短。还是需要加入人类提问才能更有意义，目前第二轮的数据都是
基于模型进行生成，效果没有那么出众。目前想到的方法是将qa和description重新进行筛选，将重复性高的问题进行删除，并且在合成的新数据
下，加入少量在第一轮用到的caption数据，防止模型遗忘caption。

84：
    有caption的预训练试试聊天效果 跑了130轮，loss下降到了0.33   要比之前好很多，能简单给出标题等
    接下游  先试用从答案索引输出的结果   slake   能到74
    slake   aug   
    
目前想到的数据集构建方式需要进一步的调整，首先简单的问答对儿使用一图一个回答的方式，不遗漏任何一个图像的问答
而推理和描述则找寻最有合适的，从而构建是一个十几万的数据集，还是有问题，预训练的detailed description应该进行重新生成，使用
caption和concepts给的多的图像进行description的生成。


重新生成description和reasoning的数据，之前的方式太笼统了   
35: 详细描述有21021条，最后生成好进行选择，选择两万条左右就够。比较重要caption  
    if len(caption.split()) > 25 and len(concepts) > 20:
34: 推理需要多一些，最后生成5万条就够。比较重视concepts   有52729
    if len(caption.split()) > 10 and len(concepts) > 35:  



重新生成：
使用14B的Qwen2.5进行生成，毕竟要保证模型效果
之前的方式生成的问答达到了20多万，太多了，使用随机的方式，只对30000个图像进行问答数据的生成，
生成9万后，再进行筛选，筛选出6万即可

qa使用4万，detailed使用1.5万，reason使用4.5万  有些过拟合了

1、重新预训练pre_caption_model   一个简单的对齐模型，仅冻结语言模型，微调vision encoder和mlp
    使用梯度累加 总计128个batch梯度更新一次   跑了19轮，loss收敛，不跑了   有些过拟合了
    只跑3轮试试，让模型别过拟合

测试了使用caption模型的效果，已经具备了caption聊天的能力
2、加载19轮的caption预训练模型进行第二阶段lora模型的预训练，训练了15轮不跑了，直接接下游

3、接下游15轮的lora模型，使用在输出位置进行loss优化的方式，model3，load_ws_answer为false
    rad   no_mix_no_aug   准确率根本升不上去


